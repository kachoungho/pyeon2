<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.pyeon2.mappers.CompanyMapper">

	<!-- 발주 목록 확인 메소드 -->
	<select id="orderList" resultType="com.pyeon2.vo.ItemVO">
		select * from orderReq order by p2_time
	</select>
	
	<!-- 발주 승인 시 해당 지점의 재고 갯수 추가해주는 메소드 -->
	<update id="updateItemCount" >
		update p2_item set count = count + #{count} where item_code = #{item_code} and area = #{area} 
	</update>
	
	<!-- 발주 승인 후 발주신청 목록 제거 메소드 -->
	<delete id="odertDelete">
		delete from orderReq where item_code = #{item_code} and area = #{area}
	</delete>
	
	<update id="orderUpdate" parameterType="com.pyeon2.vo.ItemVO">
		update company_item set count = count - #{count} where item_code = #{item_code}
	</update>
	
		<!-- 본사에서 회원 검색했을 때 관리자(admin)와 지점장(manager)만 출력 - 본사는 알바생들 관리 안하기 때문 -->
	<select id="getAdminMember" resultType="com.pyeon2.vo.MemberVO">
		select * from p2_member where position = 'admin' or position = 'manager'
	</select>
	
	<!-- 관리자/지점장 sorting 해서 리스트 출력 -->
	<select id="getPSMember" resultType="com.pyeon2.vo.MemberVO">
		select id, name, position, phone, age, address, gender, area
		from
		p2_member
		where position = #{position}
	</select>
	
	<!-- 관리자/지점장 정보 입력 -->
	<insert id="insertAdminMember" parameterType="com.pyeon2.vo.MemberVO">
		insert into p2_member(id, pwd, name, position, phone, age, address, gender, area)
		values(#{id}, #{pwd}, #{name}, #{position}, #{phone}, #{age}, #{address}, #{gender}, #{area})
	</insert>
	
	<!-- 입력된 정보를 바탕으로 권한 자동으로 주기 -->
	<insert id="insertPosition" parameterType="com.pyeon2.vo.MemberVO">
		insert into roll(id, role_name) values(#{id}, #{role_name})
	</insert>
	
	<!-- 지점별 재고 현황(물품) 불러오는 리스트 -->
	<select id="areaItemList" parameterType="com.pyeon2.vo.ItemVO" resultType="com.pyeon2.vo.ItemVO">
		select * from p2_item where area = #{area}
	</select>
	
	<!-- 아이디별 멤버 정보 불러오는 리스트 (수정하기위함) -->
	<select id="getMember" parameterType="com.pyeon2.vo.MemberVO" resultType="com.pyeon2.vo.MemberVO">
		select * from p2_member where id = #{id}
	</select>
	
	<!-- 아이디별 멤버 수정 -->
	<update id="updateMember" parameterType="com.pyeon2.vo.MemberVO">
		update p2_member set pwd = #{pwd}, name = #{name}, 
		phone = #{phone}, age = #{age}, address = #{address}, area = #{area}
		where id = #{id}
	</update>
	
	<!-- 모든 멤버 리스트 출력 -->
	<select id="getAllMember" parameterType="com.pyeon2.vo.MemberVO" resultType="com.pyeon2.vo.MemberVO">
		select * from p2_member
	</select>
	
	<!-- 멤버 정보 삭제 -->
	<delete id="deleteMember" parameterType="com.pyeon2.vo.MemberVO">
		delete from p2_member where id = #{id}
	</delete>

	<!-- 본사 전체 재고 확인 -->
	<select id="comItemListAll" resultType="com.pyeon2.vo.ComItemVO">
		select * from company_item
	</select>
	
	<!-- 본사 전체 재고 카테고리별 리스트 -->
	<select id="comItemList" resultType="com.pyeon2.vo.ComItemVO" parameterType="com.pyeon2.vo.ComItemVO">
		select * from company_item where category = #{category}
	</select>

</mapper>